import { Props } from "@blueprintjs/core";
import * as React from "react";

export class DeckView extends React.PureComponent<Props> {
  public render(): React.JSX.Element {
    return (
      <div><h1>Deck View</h1></div>
    );
  }
}
