import { Props } from "@blueprintjs/core";
import * as React from "react";

export class CollectionView extends React.PureComponent<Props> {
  public render(): React.JSX.Element {
    return (
      <div><h1>Collection View</h1></div>
    );
  }
}
