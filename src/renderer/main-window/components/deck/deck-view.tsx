import { Card, Props } from "@blueprintjs/core";
import * as React from "react";

export function DeckView(props: Props) {
  //#region Main --------------------------------------------------------------
  return (
    <Card {...props}><h1>Deck View</h1></Card>
  );
  //#endregion
}
